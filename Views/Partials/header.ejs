<!DOCTYPE html>
<html lang="en">

<head>
    <style>
        #nav-main {
            position: absolute;
            z-index: 100;
            padding: 0 2.5vw;
            background-color: white;
            /* border-bottom: 2px solid #444; */
            width: 100vw;
            margin-bottom: 0vh;
            height: 8vh;
            display: flex;
            justify-content: space-between;
        }

        #nav-main a {
            text-decoration: none;
            z-index: 90;
            color: #222222;
        }

        #nav-main-line {
            z-index: 90;
            width: 2px;
            margin: 0 1vw;
            height: 50%;
            background-color: rgb(157, 157, 157);
        }

        #left-main-nav {
            width: 25vw;
            height: 100%;
            display: flex;
            justify-content: center;
            padding: 0 1vw;
            align-items: center;
        }

        #left-main-nav a {
            font-size: 1.4vw;
            transition: all cubic-bezier(0.19, 1, 0.22, 1) 1s;
            font-weight: 600;
        }

        #right-main-nav {
            transition: all cubic-bezier(0.19, 1, 0.22, 1) 1s;
            width: 45vw;
            height: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 1.2vw;
            margin-right: 2vw;
        }

        #right-main-nav a {
            font-size: 1.2vw;
        }

        #right-main-nav>i {
            margin-left: 30px;
            font-size: 20px;
            color: #2181ff;
            z-index: 90;
            cursor: pointer;
            font-weight: 700;
        }

        #right-main-nav a:hover {
            color: #666;
        }

        #left-main-nav img {
            z-index: 90;
            height: 3.5vw;
        }

        #fullscreen-nav {
            height: 100vh;
            width: 100vw;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            background-color: #eeeeee;
            z-index: 999;
            position: absolute;
            transition: all cubic-bezier(0.19, 1, 0.22, 1) 1.5s;
            left: -100vw;
        }

        #fullscreen-nav h4 {
            font-size: 2.5vw;
            margin: 1.3vh 0;
            font-weight: 500;
            cursor: pointer;
            font-weight: 100;
        }

        #fullscreen-nav h5 {
            font-size: 2.5vw;
            position: absolute;
            cursor: pointer;
            top: 5vh;
            right: 5vw;
        }

        #fullscreen-nav h5 i {
            padding: 5px;
            font-weight: 100;
            background-color: white;
            border-radius: 50%;
        }

        #fullscreen-nav h4 a {
            text-decoration: none;
            color: #444;
            font-weight: 450;
        }

        #fullscreen-nav h4 a:hover {
            color: #000;
            font-weight: 500;
        }

        #changing-greeting-text {
            display: inline-block;
            overflow: hidden;
            height: 2.8vh;
            margin-bottom: -0.5vh;
            width: 2.8vw;
        }

        #greet-text {
            text-align: right;
            display: inline-block;
            transform: translateY(0vh);
            width: 2.7vw;
            animation: slideanimation 7s infinite alternate;
            animation-delay: 1s;
        }

#popup_box {
    width: 13vw;
    height: 10vw;
    background-color: #e4f5ff;
    position: absolute;
    border-radius: 6px;
    z-index: 99;
    top: 7vh;
    right: 7.9vw;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-direction: column;
    padding: 1.6vh 0;
    transition: all cubic-bezier(0.19, 1, 0.22, 1) 1s;
    display: none;
}

#popup_box h4 {
    font-weight: 550;
    font-size: 1.3vw;
}

#popup_box button {
    padding: 1.2vh 2vw;
    border-radius: 50px;
    border: none;
    background-color: #2baeff;
    cursor: pointer;
    box-shadow: 0px 2px 15px #2baeff70;
    transition: all cubic-bezier(0.19, 1, 0.22, 1) 0.3s;
}
#popup_box button a{
    font-size: 1.1vw;
    color: white;
    font-weight: 550;
}
#popup_box button a:hover{
    color: white;
}
#popup_box button:active {
    transform: scale(0.95);
}

@media(max-width: 600px) {
            #nav-main {
                position: relative;
                padding: 0 2.5vw;
                width: 100vw;
                margin-bottom: 3vh;
                /* background-color: rgb(255, 117, 117); */
                height: 8vh;
                display: flex;
                justify-content: space-between;
            }

            #left-main-nav {
                width: 60vw;
                /* background-color: blue; */
                height: 100%;
                display: flex;
                justify-content: center;
                padding: 0 1vw;
                align-items: center;
            }

            #right-main-nav {
                transition: all cubic-bezier(0.19, 1, 0.22, 1) 1s;
                width: 30vw;
                height: 100%;
                /* background-color: yellow; */
                display: flex;
                justify-content: center !important;
                align-items: center;
                padding: 0 1.2vw;
                margin-right: -5vw;
            }

            #right-main-nav a {
                display: none;
                white-space: nowrap;
                margin-left: -30px;
            }

            #right-main-nav a h4 {
                font-weight: 900 !important;
            }

            #left-main-nav>a {
                font-size: 11px !important;
                transition: all cubic-bezier(0.19, 1, 0.22, 1) 1s;
                font-weight: 600;
                margin-left: 0px;
                margin-top: 2px;

            }

            #nav-main-line {
                width: 2px;
                margin: 0 2vw 0 2vw;
                height: 50%;
                background-color: rgb(157, 157, 157);
            }

            #left-main-nav img {
                height: 28px;
            }

            #right-main-nav>i {
                margin-left: 30px;
                font-size: 14px;
                color: #2181ff;
                cursor: pointer;
                font-weight: 700;
            }

            #fullscreen-nav h5 {
                font-size: 4.5vw;
                position: absolute;
                cursor: pointer;
                top: 3vh;
                right: 5vw;
            }

            #fullscreen-nav h4 a {
                font-size: 4.5vw;
                margin: 1vh 0;
                font-weight: 100;
            }
        }

        @keyframes slideanimation {
            0% {
                transform: translateY(0vh);
            }
            25% {
                transform: translateY(-2.8vh);
            }
            50% {
                transform: translateY(-5.6vh);
            }
            75% {
                transform: translateY(-8.4vh);
            }
            100% {
                transform: translateY(-11.2vh);
            }
        }   
    </style>
</head>

<body>
    <div id="main">
        <div id="fullscreen-nav">
            <h4><a href="/">Home</a></h4>
            <h4><a href="/courses">Courses</a></h4>
            <h4><a href="/GirlsWhoCode">Girls Who Code</a></h4>
            <h4><a href="/campusAmbassador">Campus Ambassador</a></h4>
            <!-- <% if(typeof user != 'undefined' ){ %>
                <h4><a href="/classroom/<%= user._id %>">
                    <% } else { %>
                        <h4><a href="/SignIn">
                            <% } %>
                                <% if(typeof user != 'undefined'){ %>
                                        <span id="changing-greeting-text">
                                            <span id="greet-text">
                                                <h4>Hi</h4>
                                                <h4>Hey</h4>
                                                <h4>Oh</h4>
                                                <h4>Wow</h4>
                                                <h4>Yo</h4>
                                            </span>
                                        </span>,
                                        <%= user.name.split(' ')[0]%>
                                        <% } %>
                                <% if(typeof user === 'undefined'){ %>
                                    Sign In
                            <% } %>
                        </a></h4>  -->
            <h5 id="cross"><i class="ri-close-fill"></i></h5>
        </div>
        <div id="nav-main">
            <div id="left-main-nav">
                <img src="/images/Sheryians Logo.png" alt="">
                <div id="nav-main-line"></div>
                <a href="/">
                    <h4>Sheryians Coding School</h4>
                </a>
            </div>
            <div id="right-main-nav">
                <a href="/">
                    <h4>Home</h4>
                </a>
                <a href="/Courses">
                    <h4>Courses</h4>
                </a>
                <a href="/CampusAmbassador">
                    <h4>Campus Ambassador</h4>
                </a>
                <a href="/GirlsWhoCode">
                    <h4>Girls Who Code</h4>
                </a>
                <% if(typeof user != 'undefined' ){ %>
                    <a href="/classroom/<%= user._id %>">
                        <% } else { %>
                            <a href="/SignIn">
                                <% } %>
                                <% if(typeof user != 'undefined'){ %>
                                    <h4 id="greeting_user">
                                            <span id="changing-greeting-text">
                                                <span id="greet-text">
                                                    <h4>Hi</h4>
                                                    <h4>Hey</h4>
                                                    <h4>Oh</h4>
                                                    <h4>Wow</h4>
                                                    <h4>Yo</h4>
                                                </span>
                                            </span>,
                                            <%= user.name.split(' ')[0]%>
                                        </h4>
                                        <% } %>
                                    <% if(typeof user === 'undefined'){ %>
                                       <h4>Sign In</h4> 
                                    <% } %>
                            </a>
                            
                            <i id="menu-icon" class="ri-menu-3-line"></i>
                            <div id="popup_box">
                                <% if(typeof user === 'undefined'){ %> 
                                    <h4>Please Login</h4>
                                <% } %>
                                <% if(typeof user != 'undefined'){ %>
                                    <h4><%= user.name %> </h4>
                                <% } %> 
                                <% if(typeof user != 'undefined'){ %> 
                                <button><a href="/classroom/<%= user._id %>">My Account</a></button>
                                <% } %> 
                                <button><a href="/signIn/bye">Logout</a></button>
                            </div>
                        </div>
                    </div>

                    <script>
                        document.querySelector("#greeting_user").addEventListener("mouseenter",function(){
                        document.querySelector("#popup_box").style.display = "flex";
                        })
                        document.querySelector("#nav-main").addEventListener("mouseleave",function(){
                        document.querySelector("#popup_box").style.display = "none";
                        })
                    </script>

                    <!-- <script>                          
                        var greet_text = document.querySelectorAll('#greet-text'); 
                        let counter = 0;
                        function greetText() {
                            let counter2 = 0;
                             let s = setInterval(() => {
                                if (counter2 === 3) {
                                    clearInterval(s)
                                    counter2--;
                                    reverseGreetText();
                                }
                                counter2++;
                                counter -= 2.8;
                                greet_text.forEach((h)=>{
                                    h.style.transform = `translateY(${counter}vh)`
                                })
                                console.log("hi")
                            }, 2000);
                        }

                        function reverseGreetText() {
                            let counter2 = 0;
                            let s = setInterval(() => {
                                if (counter2 === 3) {
                                    clearInterval(s)
                                    greetText();
                                }
                                counter2++;
                                counter += 2.8;
                                greet_text.forEach((h)=>{
                                    h.style.transform = `translateY(${counter}vh)`
                                })
                                console.log("bye")
                            }, 2000);
                        }
                        greetText();
                    </script> -->